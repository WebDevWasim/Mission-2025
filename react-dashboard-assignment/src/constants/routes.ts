type RouteProps = {
  home: string;
  dashboard: string;
  settings: string;
  projects: string;
};

const routes: RouteProps = {
  home: "/",
  dashboard: "/dashboard",
  settings: "/settings",
  projects: "/projects",
};

export default routes;
